import 'package:flutter/material.dart';

TextStyle mText({
  Color textColor = Colors.black,
  FontWeight weight = FontWeight.bold,
}) {
  return TextStyle(fontSize: 12, fontWeight: weight, color: textColor);
}
